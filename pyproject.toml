[project]
# TODO: Update pyproject metadata
name = "repo-template"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "zizmor==1.5.1",
]
